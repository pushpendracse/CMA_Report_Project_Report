import SupportForm from "@/components/forms/SupportForm";

const Support = () => {
  return (
    <div className="flex min-h-screen bg-[linear-gradient(to_bottom,theme(colors.primary)_30%,theme(colors.background)_30%)] items-center justify-center font-sans p-10">
      <SupportForm />
    </div>
  )
}

export default Support;
